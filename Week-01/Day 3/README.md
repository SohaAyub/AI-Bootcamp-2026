###  Password Checker

A beginner-friendly Python program that simulates a simple login system. The program verifies whether the password entered by the user matches a predefined password and demonstrates the use of conditional statements (`if` and `else`) for decision-making.
